stages:
  - 🧪 test
  - 🔗 integration
  - 🏗️ build
  - 🚀 deploy

.node_default:
  image: node:24-alpine
  cache:
    key:
      files:
        - package.json
        - package-lock.json
    paths:
      - node_modules/

lint:
  stage: 🧪 test
  extends: .node_default
  script:
    - npm ci
    - npm run lint

test:
  stage: 🧪 test
  extends: .node_default
  script:
    - npm ci
    - npm run test:unit

integration_test:
  stage: 🔗 integration
  extends: .node_default
  script:
    - docker info